
@{
    ViewBag.Title = "Documents";
}

<!DOCTYPE html>

<html>
<head>
    <title>Documents</title>
    <script src="~/Scripts/jquery-3.4.1.js"></script>
    <script>
        $(document).ready(function () {
            $.get("/Documents/getDocuments",
                function (data, status) {
                    $("#maindiv").html(data);
                });
        });

        @*function confirmDelete(documentId) {
            var result = confirm("Are you sure you want to delete this document?");
            if (result) {
                // If user confirms, proceed with the delete operation
                deleteDocument(documentId);
            }
        }

        // Function to delete the document
        function deleteDocument(documentId) {
            $.ajax({
                url: '@Url.Action("DeleteDocument", "Documents")',
                type: 'POST',
                data: { id: documentId },
                success: function (data) {
                    // Reload the page after successful deletion
                    location.reload();
                },
                error: function (xhr, status, error) {
                    console.log('Error:', error);
                }
            });
        }*@
    </script>

    <style>
        table {
            border-collapse: collapse;
        }

        th, td {
            border: 1px solid black;
            padding: 5px;
        }

        .buttons {
            display: flex;
            flex-direction: row;
            margin-left: 280px;
        }

        #add-button {
            width: 120px;
            height: 40px;
            font-size: 15px;
            background-color: forestgreen;
            border-radius: 4px;
            color: white;
        }

        #logout {
            margin-right: 500px;
            width: 100px;
            height: 30px;
            font-size: 14px;
            background-color: red;
            border-radius: 4px;
            color: white;
            position: absolute;
            top: 30px;
            right: -10px;
        }
    </style>
</head>
<body>
    <button id="logout" onclick="window.location.href='../Authenticate/Login'">Log out</button>
    <h2>Documents</h2>
    <div id="maindiv">
    </div><br />
    <div class="buttons">
        <button id="add-button" type="button" value="Add document" onclick="window.location.href='AddDocument'">Add document</button>
    </div>
</body>
</html>



